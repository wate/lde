スキル一覧
=========================

このドキュメントは、`.github/skills/*/SKILL.md` で定義された37件のスキルを技術領域別に整理した一覧です。各スキルの「できること」と「読み込みトリガー条件」を確認し、作業内容に適したスキルを選べます。

技術領域別スキル一覧
-------------------------

### 技術開発系

| スキル名            | できること                                                                                                | 読み込みトリガー条件                                                                                                     |
| ------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `bug-fix`           | 不具合報告やテスト失敗の原因を段階的に特定し、適切な修正を実施・検証する                                  | 不具合報告を受けたとき、テストが失敗したとき                                                                             |
| `code-modification` | 修正指示(機能追加・改善・リファクタリング等)から修正箇所を洗い出し、実装・検証する                        | 機能追加・改善・リファクタリング等の修正指示があったとき(不具合報告を伴わないもの)                                       |
| `duckdb`            | DuckDBでSQL実行、集計、CSV/JSON/Parquet/XLSX変換、外部DB連携を行う                                        | 「DuckDB」「OLAP」「SQL集計」「CSV変換」「Parquet」「XLSX変換」「Excelインポート」「データ分析」「外部DB」に言及した場合 |
| `excel`             | Excelワークブックの読み書き、テーブル操作、複数シート処理、書式・数式・帳票出力を行う                     | 「Excelファイル」「Excelテーブル」「複数シート」「Workbook」「セル書式」「帳票出力」に言及した場合                       |
| `htmx`              | htmxの主要属性(hx-get/hx-post/hx-target等)を使った非同期通信・部分更新UIの実装を支援                      | 「htmx」「hx-get」「hx-post」「部分更新」「フォーム送信」「モーダル」「インライン編集」に言及した場合                    |
| `lindera`           | Rust製形態素解析ライブラリLinderaのトークナイズ、辞書切替、フィルタ、ユーザー辞書ビルド、未知語抽出を提供 | 「Lindera」「形態素解析」「トークナイズ」「分かち書き」「ユーザー辞書」「UNK」「未知語」に言及した場合                   |
| `md-links`          | Markdownドキュメントのリンク切れ検出、被リンク調査、孤立ドキュメント特定を行う                            | 「リンクチェック」「リンク切れ」「被リンク」「リンク元」「逆参照」「孤立ドキュメント」「リンク解析」に言及した場合       |
| `meilisearch`       | Meilisearchの実装・設定・運用、日本語形態素解析設定、インデックス管理、APIキー運用を支援                  | 「Meilisearch」「ファセット検索」「タイポトレランス」「検索ランキング」に言及した場合                                    |
| `web-search`        | ddgs CLIを使ったWeb/ニュース/動画/画像/書籍の6種検索とURL本文抽出を実行                                   | 最新情報・リアルタイムデータ・知識カットオフ以降の情報取得が必要な場合。回答の不確実性の裏取り時                         |
| `zx`                | zxを使った.mjsスクリプトの作成・編集、CI/CD自動化、バッチ処理、並列コマンド実行を体系的に支援             | 「zxスクリプト」「zx」「.mjsファイル」に言及した場合。`bin/*.mjs`・`.github/**/*.mjs` を対象とする作業                   |

### インフラ・運用系

| スキル名       | できること                                                                                              | 読み込みトリガー条件                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `cve-analyzer` | CVE識別子からCVSSスコア、CWE分類、PoC公開状況を3ソース(NVD API/GitHub/Exploit-DB)で自動調査             | 「CVE」「脆弱性調査」「CVSS」「PoC」に言及した場合                                             |
| `e-gov`        | e-Gov法令APIを通じて日本の法令(憲法・法律・政令・省令等)を検索・取得し、コンプライアンス検証を支援      | 「法令」「法律」「条文」「e-Gov」「コンプライアンス」「法的制約」「規制」に言及した場合        |
| `endoflife`    | endoflife.date APIで製品・ランタイム・フレームワークのEOL日付とサポート状況を正確に調査                 | 「EOL」「サポート終了」「endoflife」「まだサポートされているか」「コードネーム」に言及した場合 |
| `jgrants`      | jGrants公開APIを使って補助金・助成金情報を検索・取得し、資金調達可能性調査を支援                        | 「jGrants」「補助金」「助成金」に言及した場合                                                  |
| `memory`       | ユーザーの役割・指摘・進行中の作業・外部参照先を4カテゴリーで永続化し、セッションをまたいで想起         | 記憶すべき情報の永続化が必要な場合、是正指摘の記録、進行中タスクのコンテキスト保存時           |
| `osv-scanner`  | Google製OSV-Scannerを使い、npm/Python/Java/Go/Rust等の依存関係の既知脆弱性をスキャン                    | セキュリティ監査、CI/CDパイプライン統合、定期的なコンプライアンスチェックが必要な場合          |
| `redmine`      | Redmineのチケット・Wiki操作(作成・更新・コメント・添付)とMarkdown同期を一元化                           | 「Redmine」「チケット」「Wiki同期」に言及した場合                                              |
| `toggl`        | Toggl Track APIで時間エントリーの作成・更新・削除、タイマー操作、プロジェクト別工数レポート取得を自動化 | 「Toggl」「工数」「タイムトラッキング」「タイムエントリー」に言及した場合                      |

### 分析・設計系

| スキル名   | できること                                                                                             | 読み込みトリガー条件                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| `handoff`  | 進行中の作業進捗・未決事項・課題をMarkdownで構造化し、次セッションで即座に再開できる引き継ぎ資料を生成 | 「引き継ぎ資料を作って」「handoff」、作業中断前の要点整理、会話の長期化によるセッション切り替え時 |
| `hearing`  | ソクラテス式問答法と構造化ヒアリングシートで曖昧な依頼の前提条件や潜在ニーズを引き出す                 | 目的や背景が曖昧な依頼(機能追加・要件定義・障害調査・事業検討等)を受けた場合                      |
| `review`   | 全フェーズの成果物をフェーズ別チェックリストで評価し、5段階重要度で分類した構造化フィードバックを提供  | 「レビュー」「見てほしい」「確認して」「問題点を洗い出して」に言及した場合                        |
| `thinking` | 曖昧な問いかけに対し、質問の意図整理->思考フェーズ判定->フレームワーク候補の優先順決定を行う           | 曖昧な問いかけや「どう考えるべきか」を問うメッセージがあった場合                                  |
| `workplan` | 情報密度判定とWBS分解に基づき、再開可能で検証可能な作業プランを生成                                    | 「作業プランを作って」「workplan」「タスクを分解したい」「WBSで整理したい」に言及した場合         |

### デザイン・UX系

| スキル名            | できること                                                                                                     | 読み込みトリガー条件                                                                                                            |
| ------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `adminlte3-ui`      | AdminLTE 3の標準コンポーネントに沿ってWeb画面を構成し、Bootstrap 4前提のレイアウト規約・コード例を提供         | 「AdminLTE」「AdminLTE 3」「adminlte」「content-wrapper」「small-box」「info-box」に言及した場合                                |
| `bootstrap4-ui`     | Bootstrap 4の標準コンポーネントに沿ってWeb画面を構成し、Bootstrap 5との混在を防止                              | 「Bootstrap 4」「Bootstrap4」「bootstrap.css」「bs4」に言及した場合                                                             |
| `bulma-ui`          | Bulma標準class(columns/field/box/card/table等)に沿ってWeb画面を構成し、Bootstrap系classの混入を防止            | 「Bulma」「フォーム」「テーブル」「モーダル」「レイアウト」に言及した場合                                                       |
| `d2`                | D2言語でシステムフロー図・データフロー図・シーケンス図・アーキテクチャ図等を作成・編集・解析                   | 「D2」「.d2ファイル」「テキストベースの図表」に言及した場合                                                                     |
| `design-token`      | スライドやWebサイト向けのデザイントークン(カラー・タイポグラフィ・スペーシング等)を定義・管理                  | slide-workflow または website-workflow のテーマ設計フェーズ、デザイントークン定義の明示的依頼時                                 |
| `drawio`            | draw.io XML形式でシステムフロー図・UML図等を作成・編集・解析し、PNG/SVG/PDFにエクスポート                      | 「draw.io」「drawio」「diagrams.net」「.drawio」に言及した場合                                                                  |
| `graphic-creator`   | デザイン哲学の作成から始め、ポスター・バナー・インフォグラフィック等のビジュアル素材を生成                     | 「画像を作って」「バナーを作って」「インフォグラフィックを作って」「イラストを作って」に言及した場合                            |
| `image-manipulator` | 既存画像ファイルのリサイズ・クロップ・フォーマット変換・色調補正・透かし合成・Exif操作等を実行                 | 「画像を加工したい」「リサイズ」「フォーマット変換」「透かし」「差分画像」に言及した場合                                        |
| `mockup`            | NunjucksテンプレートからHTMLをビルドし、開発サーバー起動・監視を行う                                           | 「mockup」「モックアップビルド」「mockup.mjs」「mockup-server」「Nunjucksビルド」に言及した場合                                 |
| `screen-flow`       | 業務フロー/サービスフローからユーザーフローを抽出し、画面遷移図へ段階的に落とし込む設計プロセスを提供          | 「画面遷移」「フロー設計」「画面構成」「ユーザーフロー」「遷移図」に言及した場合                                                |
| `slide-structure`   | 9種類のパターンから適切なスライド構成を選定し、タイトル・目的・内容・レイアウトを揃えた構成案を提案            | 「構成を考えたい」「ページ構成を相談したい」「スライドの流れを見直したい」の明示的依頼、slide-workflowのフェーズ3からの呼び出し |
| `slide-workflow`    | hearing/slide-structure/pptx/design-token/review等を連携させ、ヒアリングからスライド生成までを段階的に実行     | 「スライドを作りたい」「プレゼン資料を作って」「ピッチデッキを作りたい」等のスライド新規作成依頼                                |
| `ui-pattern`        | 画面を構成するUIパターンのコンセプト・機能・レイアウト・振る舞いの選定指針を提供                               | 「コンポーネント」「フォーム」「一覧」「フィルタリング」「モーダル」「タブ」「ウィザード」に言及した場合                        |
| `website-workflow`  | hearing/screen-flow/design-token/ui-pattern/mockup/review等を連携させ、Webサイトを企画から実装まで段階的に生成 | 「ウェブサイトを作りたい」「サイトを作って」「ランディングページを作って」「HPを作りたい」に言及した場合                        |

使い分けの指針
-------------------------

### 利用シーン別の選択戦略

- 要件が曖昧な開始時: `hearing` を優先して目的と制約を明確化します。
- 計画が必要な場合: `workplan` で作業をWBS分解し、実行可能なタスク構造にします。
- **品質確認が必要な場合**: `review` で成果物を5段階重要度で評価します。
- 実装作業の修正: `code-modification`(機能追加・改善)と `bug-fix`(不具合修正)を使い分けます。
- データ分析・変換: `duckdb` で大規模データを処理し、`excel` で帳票を出力します。
- 画像制作: `graphic-creator` で新規生成、`image-manipulator` で既存画像を加工します。

### ワークフロー型スキルの連鎖

上位のワークフロースキルは、実行フェーズに応じて下位スキルを連鎖的に読み込みます。

- スライド制作: `slide-workflow` -> `hearing` / `slide-structure` / `design-token` / `pptx`
- Webサイト制作: `website-workflow` -> `screen-flow` / `design-token` / `ui-pattern` / `mockup`
- 不具合修正: `bug-fix` -> `review`(修正後の検証)
- 修正指示対応: `code-modification` -> `review`(修正後のコードレビュー)

### セキュリティ・コンプライアンス対応

- 脆弱性スキャン: `osv-scanner` で依存関係全体を把握 -> 必要なCVEのみ `cve-analyzer` で深掘り
- 法令確認: `e-gov` で日本の法令を検索・取得し、コンプライアンス検証
- EOL確認: `endoflife` で使用製品のサポート終了日を確認
- 補助金調査: `jgrants` で補助金・助成金情報を検索

### 運用継続での履歴管理

`review` で指摘を整理 -> `memory` で判断軸と経緯を保存 -> `handoff` で次回再開情報を引き継ぐ、の順序で運用します。

効率化のポイント
-------------------------

### 探索系と実行系の分離

調査・探索系(`web-search`、`lindera` 等)と実行・実装系(`zx`、UI系スキル等)を分けて使うと、不要なスキル読み込みを減らせます。

### user-invocable スキルの活用

以下のスキルはユーザーから明示的に呼び出せます(`user-invocable: true`)。要件に応じて直接指定してください。

`bug-fix`・`code-modification`・`design-token`・`graphic-creator`・`handoff`・`image-manipulator`・`md-links`・`memory`・`review`・`slide-structure`・`slide-workflow`・`toggl`・`web-search`・`website-workflow`・`workplan`

### ワークフロースキル起点の選択

新規作成系の作業は、上位のワークフロースキル(`slide-workflow`、`website-workflow`)を起点にすると、必要な下位スキルが自動連鎖的に読み込まれます。

まとめ
-------------------------

本一覧は、37件のスキルを4分類で網羅し、全スキルに「できること」と「読み込みトリガー条件」を記載しました。実作業では、まず利用シーンを決め、対応するワークフロー型または単機能型スキルを起点に選択します。この手順で、読み込み漏れと過剰読み込みの両方を抑制できます。
