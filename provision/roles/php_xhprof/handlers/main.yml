---
- name: restart php-fpm
  ansible.builtin.systemd:
    name: php{{ php_version | default('') | string }}-fpm
    state: restarted
    enabled: true
  when: "'php' + (php_version | default('') | string) + '-fpm' in php_packages | default([])"
