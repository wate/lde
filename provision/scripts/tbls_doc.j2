#!/bin/sh
mkdir -p /vagrant/output/schema

TBLS_PARAMS=''
if [ -f /vagrant/.tbls.yml ]; then
  TBLS_PARAMS='--config /vagrant/.tbls.yml'
else
  if [ -f /vagrant/tbls.yml ]; then
    TBLS_PARAMS='--config /vagrant/tbls.yml'
  fi
fi
/usr/bin/tbls doc ${TBLS_PARAMS} -f
