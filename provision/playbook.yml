- name: setup local development environment
  hosts: all
  become: yes
  roles:
    - common
    - mariadb
    - apache
    - php
    - mailcatcher
    - phpmyadmin
    - wp-cli
  tasks:
    - name: restart service
      systemd:
        name: "{{ item }}"
        state: restarted
      with_items:
        - httpd
        - mariadb
